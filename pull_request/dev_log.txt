================================================================================
  PullRequest — Development Log
  Playlist Transfer App (Spotify ↔ YouTube Music)
================================================================================

PURPOSE
-------
This file serves as a running record of architectural decisions, feature
implementations, and technical rationale. Since the project does not currently
use Jira or a user-stories tool, this document captures what was built, why it
was built that way, and what is intentionally left as a stub for future work.


================================================================================
  INITIAL STATE AUDIT  (completed: 2026-02-22)
================================================================================

The codebase had a working Flutter skeleton with:
  - Static screens connected via imperative Navigator.push chains
  - Services (PlatformService, PlaylistService, TransferService) instantiated
    directly inside widget State classes — no sharing, no injection
  - No state management layer (all setState)
  - No routing abstraction
  - All playlist data served from local JSON assets (mock data)
  - OAuth for Spotify and YouTube Music not yet implemented — authorization
    screen just simulated a 1-second delay and returned success
  - Transfer logic simulated via an async generator with in-memory delays

PROBLEMS IDENTIFIED
  1. No state management: each screen created its own service instance,
     meaning PlaylistService cache was destroyed on every navigation pop.
  2. No repository abstraction: PlaylistService was tightly coupled to local
     JSON assets via a switch/case per platform ID. Replacing it with a real
     API would require rewriting the service entirely.
  3. Platform model name collision: class Platform in models/ shadowed
     dart:io's Platform, causing potential confusion.
  4. TransferProgress.copyWith bug: currentSongTitle used the ?? operator,
     making it impossible to explicitly reset it to null.
  5. Dead code: PlaylistSelectionScreen._handleContinue() showed a SnackBar
     and immediately popped — the SnackBar was never visible.
  6. Home screen hardcoded: Spotify and YouTube Music icons were hardcoded
     in home_screen.dart, not driven by PlatformService.
  7. Sequential navigation debt: PlatformSelectionScreen chained four
     Navigator.push calls, making the flow fragile and untestable.
  8. No named routing, no deep-link support.
  9. No typed error hierarchy — errors surfaced as raw exception strings.


================================================================================
  ARCHITECTURE DECISIONS
================================================================================

STATE MANAGEMENT — provider package
  Chosen over Riverpod and Bloc for pragmatic reasons:
  - Provider is officially recommended for this scale of app
  - Minimal boilerplate, easy to swap to Riverpod later if complexity grows
  - ChangeNotifier pattern maps cleanly to the three domains:
      AuthProvider    — per-platform OAuth state
      PlaylistProvider — playlist loading, caching, and selection state
      TransferProvider — active transfer stream subscription and progress

ROUTING — go_router package
  Chosen over Navigator 2.0 raw API:
  - Declarative route table, easy to add deep links when needed
  - context.push() returns a Future, preserving the wizard-like flow
    (await authorization → await playlist selection → go to transfer)
  - Route argument classes (AuthorizationArgs, TransferArgs) make the
    navigation contract explicit and type-safe

REPOSITORY PATTERN
  Each data domain has an abstract interface:
      AuthRepository       — authorize(platform), revokeAuth, isAuthorized
      PlaylistRepository   — fetchPlaylists(platformId), fetchPlaylistDetails
      TransferRepository   — transferPlaylists(playlists, src, dst) → Stream

  Mock implementations live in lib/services/ and are wired in main.dart.
  Swapping to real Spotify/YTMusic APIs = implement the interface + change
  one line in main.dart. Zero screen or provider changes required.

NAMING — MusicPlatform
  The model formerly called Platform was renamed to MusicPlatform to avoid
  shadowing dart:io's Platform class. The model was also extended with:
    - brandColor: Color  (Spotify green, YouTube red — for UI accents)
    - oauthClientId: String?  (null until OAuth credentials are configured)
    - scopes: List<String>  (OAuth permission scopes, defined per platform)

THEME
  AppTheme.light() returns a full ThemeData used in MaterialApp.router.
  AppTextStyles provides named constants (heading1, heading2, body, label,
  caption) so text sizing is consistent and not duplicated per screen.
  The AppBar style, ElevatedButton style, and color scheme are defined once.

ERROR HANDLING
  AppException (abstract) is the base. Subtypes:
    AuthException, PlaylistException, TransferException, NetworkException
  Services throw typed exceptions; providers catch and expose them as
  String? errorMessage fields that the UI reads.


================================================================================
  FEATURE IMPLEMENTATION NOTES
================================================================================

AUTH FLOW (stub, ready for OAuth)
  PlatformAuthorizationScreen calls AuthProvider.authorize(platform).
  MockAuthService simulates a 1.5-second delay and returns success.
  When real OAuth is implemented:
    1. Create SpotifyAuthService implements AuthRepository
    2. Implement authorize() using flutter_web_auth_2 or app_links for
       the OAuth redirect callback
    3. Store tokens via flutter_secure_storage
    4. Wire SpotifyAuthService (and YtMusicAuthService) in main.dart
  No screen changes are needed — only the injected implementation changes.

PLAYLIST LOADING (stub, ready for API)
  MockPlaylistService reads from assets/data/*.json.
  PlaylistProvider caches by platformId so JSON is only parsed once.
  When real API is implemented:
    1. Create SpotifyPlaylistService implements PlaylistRepository
    2. Inject access token from AuthProvider
    3. Call GET /v1/me/playlists with pagination
    4. Wire in main.dart
  PlaylistProvider's loadPlaylists() and selectedPlaylists already handle
  multi-platform state — no provider changes needed.

TRANSFER (stub, ready for API)
  MockTransferService simulates per-song progress with delays.
  When real transfer is implemented:
    1. Create RealTransferService implements TransferRepository
    2. For each song: search destination API, add to playlist
    3. Yield TransferProgress updates
    4. Wire in main.dart
  TransferProvider and PlaylistTransferScreen need zero changes.

TRANSFER HISTORY
  TransferHistoryScreen is a stub with an empty state UI.
  A TransferRecord model and a local persistence layer (e.g. sqflite or
  Hive) will need to be added to populate this screen.
  Suggested model fields:
    - id, timestamp, sourcePlatformId, destinationPlatformId
    - totalSongs, transferredSongs, status, playlistNames

MISSING VIEWS (to be implemented)
  - Settings screen (API keys, account connections, theme toggle)
  - Per-playlist transfer result detail (which songs were found/missed)
  - Onboarding / first-run screen
  - Error recovery UI for partial transfer failures


================================================================================
  JIRA BACKLOG
================================================================================

EPIC  PULL-10 — Spotify OAuth Integration
  Goal: Replace MockAuthService with a real Spotify PKCE OAuth flow so users
  can connect their Spotify account and the app can read their playlists.

  ┌─────────────────────────────────────────────────────────────────────────┐
  │ USER STORIES                                                            │
  ├─────────────────────────────────────────────────────────────────────────┤
  │ PULL-11  As a user, I want to connect my Spotify account so that the   │
  │          app can read my private and collaborative playlists.           │
  │          Acceptance:                                                    │
  │            - Tapping "Authorize Spotify" opens the Spotify login page  │
  │            - After granting permission, I am returned to the app       │
  │            - The authorization screen transitions to the next step     │
  │                                                                         │
  │ PULL-12  As a user, I want my Spotify session to persist across app    │
  │          restarts so I don't have to re-authorize every time.          │
  │          Acceptance:                                                    │
  │            - Killing and relaunching the app does not prompt OAuth     │
  │            - isAuthorized('spotify') returns true on relaunch          │
  │            - Access token is refreshed silently when expired           │
  │                                                                         │
  │ PULL-13  As a user, I want the app to handle a cancelled or failed     │
  │          authorization gracefully, with a clear error message and the  │
  │          ability to retry.                                              │
  │          Acceptance:                                                    │
  │            - Closing the browser mid-flow does not crash the app       │
  │            - AuthProvider.errorFor('spotify') surfaces the message     │
  │            - The "Authorize" button is re-enabled after failure        │
  └─────────────────────────────────────────────────────────────────────────┘

  TASKS (sub-tasks of PULL-10)
  ─────────────────────────────────────────────────────────────────────────
  PULL-14  Add dependencies: flutter_web_auth_2, flutter_secure_storage, http
             Type: Task | Points: 1
             File: pubspec.yaml

  PULL-15  Register custom URI scheme pullrequest://callback on Android/iOS
             Type: Task | Points: 2
             Files:
               android/app/src/main/AndroidManifest.xml
               ios/Runner/Info.plist
             Notes: Also register the redirect URI in the Spotify Developer
             Dashboard. Add android:launchMode="singleTop" to <activity>.

  PULL-16  Implement SpotifyAuthService with PKCE authorization flow
             Type: Story | Points: 5
             File: lib/services/spotify_auth_service.dart
             Implements: AuthRepository
             Details:
               - PKCE: generate codeVerifier (32 random bytes, base64url) and
                 codeChallenge (SHA-256, base64url no-padding)
               - Launch browser via FlutterWebAuth2.authenticate()
               - Exchange auth code for tokens via POST /api/token
               - Store access_token, refresh_token, expiry_ms in
                 FlutterSecureStorage
               - isAuthorized(): check token existence + expiry, silent refresh
               - revokeAuth(): delete all 3 storage keys
               - _refreshTokens(): silent refresh_token grant
               - Expose getAccessToken() for future SpotifyPlaylistService

  PULL-17  Wire SpotifyAuthService in main.dart
             Type: Task | Points: 1
             File: lib/main.dart
             Notes: Replace MockAuthService() — one line change. Keep mock
             import for test isolation.

  PULL-18  Unit tests for SpotifyAuthService
             Type: Task | Points: 3
             File: test/services/spotify_auth_service_test.dart
             Coverage:
               - authorize() success path returns AuthorizationResult.success
               - authorize() user-cancel returns AuthorizationResult.failure
               - authorize() network error throws AuthException
               - isAuthorized() returns false when no token stored
               - isAuthorized() returns false when token expired and refresh fails
               - isAuthorized() returns true when token expired but refresh succeeds
               - revokeAuth() clears all storage keys


================================================================================
  JIRA BACKLOG (continued)
================================================================================

EPIC  PULL-20 — Playlist Loading & Selection Flow
  Goal: Wire the playlist screens so that, after OAuth, the user's real
  playlists load correctly. The lazy detail-fetch pattern ensures the code
  is ready for real Spotify/YouTube Music APIs where track lists are a
  separate API call from the playlist catalogue.

  ┌─────────────────────────────────────────────────────────────────────────┐
  │ USER STORIES                                                            │
  ├─────────────────────────────────────────────────────────────────────────┤
  │ PULL-21  As a user, I can see my playlists from the source platform     │
  │          listed after I authorize the app.                              │
  │          Acceptance:                                                    │
  │            - PlaylistSelectionScreen shows a skeleton while loading     │
  │            - Playlists appear with name, track count, description       │
  │            - An error message + Retry button is shown on failure        │
  │                                                                         │
  │ PULL-22  As a user, I can tap a playlist and view its full track list   │
  │          with titles, artists, and durations.                           │
  │          Acceptance:                                                    │
  │            - Tapping the info button on a PlaylistCard opens details    │
  │            - A LoadingSongCard skeleton shows while tracks fetch        │
  │            - Songs are listed in order with track number, title,        │
  │              artist, optional album, and formatted duration             │
  │            - An error message + Retry button shows on failure           │
  │                                                                         │
  │ PULL-23  As a user, I can search and filter my playlists by name or    │
  │          description so I can quickly find what I want to transfer.     │
  │          Acceptance:                                                    │
  │            - A search bar filters the list in real time                 │
  │            - Clearing search restores the full list                     │
  │            - "No playlists found" empty state shown when no match       │
  └─────────────────────────────────────────────────────────────────────────┘

  TASKS (sub-tasks of PULL-20)
  ─────────────────────────────────────────────────────────────────────────
  PULL-24  Extend PlaylistProvider with playlist-detail loading state      ✓
             Type: Task | Points: 2
             File: lib/providers/playlist_provider.dart
             Details:
               - Add _details, _loadingDetails, _detailErrors maps
               - Add detailsFor(), isLoadingDetails(), detailErrorFor()
               - Add loadPlaylistDetails(platformId, playlistId,
                 {forceRefresh}) with same cache / error pattern as
                 loadPlaylists()
               - clearCache() now also resets detail cache and errors

  PULL-25  Add LoadingSongCard skeleton widget                             ✓
             Type: Task | Points: 1
             File: lib/widgets/common/loading_song_card.dart
             Details:
               - Animated opacity shimmer matching SongCard dimensions
               - Track-number box, two text-line placeholders, duration
               - Follows exact same implementation pattern as
                 LoadingPlaylistCard

  PULL-26  Refactor PlaylistDetailsScreen to fetch tracks lazily           ✓
             Type: Story | Points: 3
             File: lib/screens/playlist_details_screen.dart
             Details:
               - Convert StatelessWidget → StatefulWidget
               - initState triggers loadPlaylistDetails via PlaylistProvider
               - Consumer<PlaylistProvider> drives isLoading / error / data
               - Falls back to metadata playlist (name, description) while
                 loading so the header renders immediately
               - ErrorView + forceRefresh retry wired to provider

  PULL-27  Unit tests for PlaylistProvider detail-loading state            ✓
             Type: Task | Points: 3
             File: test/providers/playlist_provider_test.dart
             Coverage:
               - loadPlaylistDetails stores result / cache hit / forceRefresh
               - isLoadingDetails true during fetch, false after
               - detailErrorFor set on PlaylistException, null on success
               - clearCache resets list data, detail data, and detail errors
               - Existing loadPlaylists caching and error paths validated


================================================================================
  DEPENDENCY MANIFEST
================================================================================

  provider: ^6.1.2
    State management via ChangeNotifier + MultiProvider

  go_router: ^14.6.3
    Declarative URL-based routing

  (planned — PULL-14, not yet added)
  flutter_secure_storage: ^9.2.2
    Encrypted token persistence for OAuth access/refresh tokens

  (planned — PULL-14, not yet added)
  flutter_web_auth_2: ^4.0.1
    OAuth browser launch + redirect callback interception via custom URI scheme
    Chosen over app_links: handles both launch and callback in one call

  (planned — PULL-14, not yet added)
  http: ^1.2.0
    Token exchange HTTP calls (POST /api/token, refresh grant)


================================================================================
  FOLDER STRUCTURE
================================================================================

  lib/
  ├── core/
  │   ├── errors/
  │   │   └── app_exception.dart        Typed exception hierarchy
  │   ├── router/
  │   │   └── app_router.dart           GoRouter config + route constants
  │   └── theme/
  │       ├── app_colors.dart           Color palette (unchanged)
  │       ├── app_text_styles.dart      Named text style constants
  │       └── app_theme.dart            Full ThemeData factory
  ├── models/
  │   ├── music_platform.dart           Renamed from platform.dart
  │   ├── playlist.dart
  │   ├── song.dart
  │   ├── authorization_result.dart
  │   ├── authorization_state.dart
  │   └── transfer_progress.dart
  ├── repositories/                     Abstract interfaces
  │   ├── auth_repository.dart
  │   ├── playlist_repository.dart
  │   └── transfer_repository.dart
  ├── services/                         Concrete implementations (mock)
  │   ├── platform_service.dart         Static config, no interface needed
  │   ├── mock_auth_service.dart
  │   ├── mock_playlist_service.dart
  │   └── mock_transfer_service.dart
  ├── providers/                        ChangeNotifier state holders
  │   ├── auth_provider.dart
  │   ├── playlist_provider.dart
  │   └── transfer_provider.dart
  ├── screens/
  │   ├── home_screen.dart
  │   ├── platform_selection_screen.dart
  │   ├── platform_authorization_screen.dart
  │   ├── playlist_selection_screen.dart
  │   ├── playlist_details_screen.dart
  │   ├── playlist_transfer_screen.dart
  │   └── transfer_history_screen.dart  Stub
  ├── widgets/
  │   ├── common/
  │   │   ├── error_view.dart           Reusable error + retry widget
  │   │   ├── loading_playlist_card.dart Skeleton loader (list view)
  │   │   └── loading_song_card.dart    Skeleton loader (detail view)
  │   ├── platform_card.dart
  │   ├── platform_selector.dart
  │   ├── playlist_card.dart
  │   └── song_card.dart
  ├── app.dart                          MaterialApp.router + AppTheme
  └── main.dart                         MultiProvider injection root


================================================================================
  COMMIT HISTORY GUIDE
================================================================================

  docs: add development log and project documentation
  refactor: rename Platform to MusicPlatform and extend with brand config
  fix: correct TransferProgress copyWith nullable field handling
  feat: add provider and go_router dependencies
  feat: introduce repository pattern with abstract interfaces and typed exceptions
  feat: implement mock services conforming to repository contracts
  feat: add ChangeNotifier providers for auth, playlist, and transfer state
  feat: centralize theme, text styles, and introduce GoRouter navigation
  feat: add reusable ErrorView and skeleton loading widget
  refactor: update screens to consume providers and GoRouter
  feat: add transfer history screen stub
  test: update tests for renamed models, services, and providers

  ── Spotify OAuth (PULL-10) ──────────────────────────────────────────────
  docs: add Jira backlog and user stories for Spotify OAuth epic (PULL-10)
  feat(PULL-14): add flutter_web_auth_2, flutter_secure_storage, http deps
  feat(PULL-15): register pullrequest://callback URI scheme on Android + iOS
  feat(PULL-16): implement SpotifyAuthService with PKCE authorization flow
  feat(PULL-17): wire SpotifyAuthService in main.dart replacing MockAuthService
  test(PULL-18): add unit tests for SpotifyAuthService token lifecycle

  ── Playlist Loading (PULL-20) ───────────────────────────────────────────
  feat(PULL-24): extend PlaylistProvider with playlist-detail loading state
  feat(PULL-25): add LoadingSongCard skeleton widget
  feat(PULL-26): refactor PlaylistDetailsScreen to fetch tracks lazily via provider
  test(PULL-27): add PlaylistProvider detail-loading unit tests
  docs: add PULL-20 playlist-loading epic to dev_log

================================================================================
